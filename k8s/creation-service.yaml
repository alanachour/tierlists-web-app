apiVersion: v1
kind: Service
metadata:
  name: tierlist-backend-service
  labels:
    app: tierlist-backend
spec:
  selector:
    app: tierlist-backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
